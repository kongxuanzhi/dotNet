using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;

namespace RecruCol.CommonHelp.DataConver
{
    public class addClassToCookie
    {
        //private static void NewMethod(HttpContext context, string Search, object check)
        //{
        //    Type t = check.GetType();
        //    if (!string.IsNullOrEmpty(Search))
        //    {
        //        foreach (var property in t.GetProperties())
        //        {
        //            check.map[property.Name] = context.Request[property.Name] == null ? "" : context.Request[property.Name];
        //            context.Response.SetCookie(new HttpCookie(property.Name.ToUpper(), check.map[property.Name].ToString()));
        //        }
        //    }
        //    else
        //    {
        //        foreach (var property in t.GetProperties())
        //        {
        //            check.map[property.Name] = context.Request.Cookies[property.Name] == null ? "" : context.Request.Cookies[property.Name].Value;
        //        }
        //    }
        //}
        
    }
}